<%= form_for(@caller) do |f| %>
  <% if @caller.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@caller.errors.count, "error") %> prohibited this caller from being saved:</h2>

      <ul>
      <% @caller.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :number %><br />
    <%= f.text_field :number %>
  </div>
  <div class="field">
    <%= f.label :last_call_at %><br />
    <%= f.datetime_select :last_call_at %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
